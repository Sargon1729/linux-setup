# Linux-setup
### This script is not idempotent!
Linux initial set up. This is truly the beginning of something great.

To prevent a package from installation simply prepend a hashtag to it to it `#openssh-server`.

Simply copy the `config.sh.example` and rename `config.sh`, then run the main script as root.

